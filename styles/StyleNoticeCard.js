import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    padding: 15,
    marginBottom: 0.5,
    elevation: 3,
    marginTop: 0.5,
  },
  title: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#000',
  },
  message: {
    fontSize: 10,
    color: '#000',
  },
});

export default styles;
